<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.mygoodsupporter.mapper.ProjectDAO">

	<resultMap id="ProjectResultMap" type="Project">
		<id property="id" column="id"/>
		<result property="userId" column="user_id"/>
		<result property="name" column="name"/>
		<result property="slug" column="slug"/>
		<result property="targetAmount" column="target_amount"/>
		<result property="currentAmount" column="current_amount"/>
		<result property="content" column="content"/>
		<result property="status" column="status"/>
	</resultMap>

	<select id="getProjects" resultMap="ProjectResultMap">
		SELECT id, member_id, name, slug, target_amount, current_amount, content, status
		FROM project
	</select>

	<select id="getProjectById" resultMap="ProjectResultMap">
		SELECT id, member_id, name, slug, target_amount, current_amount, content, status
		FROM project
		WHERE id = #{id}
	</select>

	<select id="getProjectBySlug" parameterType="String" resultMap="ProjectResultMap">
		SELECT id, member_id, name, slug, target_amount, current_amount, content, status
		FROM project
		WHERE slug= #{slug}
	</select>

	<insert id="insert" parameterType="Project">
		INSERT INTO project
		    (id, member_id, name, slug, target_amount, current_amount, content, status)
		VALUES
		    (nextval(project_seq), #{memberId}, #{name}, #{slug}, #{targetAmount}, #{currentAmount}, #{content}, #{status})
	</insert>

	<update id="update" parameterType="Project">
		UPDATE project SET
						   user_id = #{userId},
						   name = #{name},
						   target_amount = #{targetAmount},
						   current_amount = #{currentAmount},
						   content = #{content},
						   status = #{status}
		WHERE id = #{id}
	</update>

</mapper>