<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.github.mygoodsupporter.dao.MemberDAO">
	<resultMap id="MemberResultMap" type="Member">
		<result property="id" column="m_id"/>
		<result property="password" column="m_password"/>
		<result property="name" column="m_name"/>
		<result property="phone" column="m_phone"/>
		<result property="email" column="m_email"/>
		<collection property="authorities" ofType="Authority">
			<result property="memberId" column="m_id"/>
			<result property="authority" column="authority"/>
		</collection>
	</resultMap>

	<!-- 회원가입 -->
	<insert id="insertMember" parameterType="Member">
		INSERT INTO member
			(m_id, m_password, m_name, m_phone, m_email)
		VALUES (#{id}, #{password}, #{name}, #{phone}, #{email})
	</insert>

	<select id="getMemberById" parameterType="String" resultMap="MemberResultMap">
		SELECT m.m_id       as m_id,
			   m.m_password as m_password,
			   m.m_name     as m_name,
			   m.m_phone    as m_phone,
			   m.m_email    as m_email,
			   a.authority  as authority
		FROM member m
				 LEFT OUTER JOIN authority a ON m.m_id = a.m_id
		WHERE m.M_ID = #{id};
	</select>

	<insert id="insertAuthority" parameterType="Authority">
		INSERT INTO authority
			(m_id, authority)
		VALUES (#{memberId}, #{authority})
	</insert>

	<!-- 회원정보 수정	요청 -->
	<select id="select" parameterType="String" resultMap="MemberResultMap">
		SELECT * FROM member where m_id=#{id}
	</select>

	<!-- 회원정보 수정	-->
	<update id="update" parameterType="Member">
		update member set m_phone=#{phone}, m_email=#{email} where m_id=#{id}
	</update>

	<!-- 회원목록 조회 -->
	<select id="list" resultMap="MemberResultMap">
		select * from member
	</select>
</mapper>